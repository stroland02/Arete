{
  "name": "beancount-sandbox",
  "version": "0.1.0",
  "private": true,
  "description": "Fake customer app (TEST DATA) used to exercise Kuma end-to-end. Not part of the Areté build.",
  "type": "module",
  "scripts": {
    "start": "node --experimental-strip-types api/server.ts",
    "test": "node --test --experimental-strip-types"
  }
}
