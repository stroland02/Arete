{
  "id": "quality-mutable-default-arg",
  "pr": {
    "repo": "acme/api",
    "pr_number": 117,
    "title": "Add cart item helper",
    "description": "Adds an item to a cart list.",
    "files": [
      {
        "path": "src/cart/cart.py",
        "patch": "@@ -0,0 +1,3 @@\n+def add_item(item, items=[]):\n+    items.append(item)\n+    return items\n+",
        "additions": 3,
        "deletions": 0
      }
    ]
  },
  "planted_defects": [
    {
      "id": "mutabledefault-001",
      "path": "src/cart/cart.py",
      "line": 1,
      "target_agent": "quality",
      "description": "Mutable default argument: 'items=[]' is evaluated once at function definition time and shared across every call that omits items, so unrelated callers silently accumulate items into the same list instead of getting a fresh one.",
      "severity": "warning"
    }
  ],
  "clean": false
}
